depthai
git+https://github.com/luxonis/depthai-nodes.git@main#egg=depthai-nodes
opencv-python
numpy

# NOTE: rclpy and vision_msgs are ROS 2 system packages.
# Install them on the Jetson via:
#   sudo apt install ros-humble-vision-msgs
# and source your ROS 2 workspace before running the publisher node.
# They are not listed here because pip cannot install them.
